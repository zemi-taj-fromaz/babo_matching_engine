# Build output
build*/
cmake-build-*/

# CLion / IDE
.idea/

# Cross-machine paper data: keep the unzipped bundle folders, not the raw ZIPs
# (the ZIP just duplicates the folder the aggregator reads).
paper/data/**/*.zip
